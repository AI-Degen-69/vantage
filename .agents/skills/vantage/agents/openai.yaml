interface:
  display_name: "Vantage"
  short_description: "Repo-specific patterns and workflows for vantage"
  default_prompt: "Use the vantage repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true